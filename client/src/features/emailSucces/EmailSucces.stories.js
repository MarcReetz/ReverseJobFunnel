import EmailSucces from "./EmailSucces";

export default {
  name: "EmailSucces",
  component: EmailSucces
}

const Template = (args) => <EmailSucces {...args}/>

export const First = Template.bind()

First.args = {

}