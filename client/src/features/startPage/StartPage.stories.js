import StartPage from "./StartPage";

export default {
  name:"StartPage",
  component: StartPage
}

const Template = (args) => <StartPage {...args}/>;

export const First = Template.bind({})

First.args = {
  
}
