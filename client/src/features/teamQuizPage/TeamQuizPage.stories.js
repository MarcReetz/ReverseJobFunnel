import TeamQuizPage from "./TeamQuizPage";

export default {
  name: 'TeamQuizPage',
  component: TeamQuizPage
}

const Template = (args) => <TeamQuizPage/>

export const First = Template.bind()

First.args = {
  
}

