import ConcentrationQuizPage from "./ConcentrationQuizPage";

export default {
  name: 'ConcentrationQuizPage',
  component: ConcentrationQuizPage
}

const Template = (args) => <ConcentrationQuizPage/>

export const First = Template.bind()

First.args = {
  
}

