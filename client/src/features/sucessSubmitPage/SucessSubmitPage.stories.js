import SucessSubmitPage from "./SucessSubmitPage";

export default {
  name:"SuccessSubmitPage",
  component:SucessSubmitPage
}

const Template = (args) => <SucessSubmitPage/>

export const First = Template.bind()

First.args = {
  
}